const data = require('../data/zoo_data');

// const buscaNome = (nome) =>
//   data.employees.find(({ firstName, id }) => firstName === nome || id === nome)
//     .firstName;
// const sname = (nome) =>
//   data.employees.find(({ firstName, id }) => firstName === nome || id === nome)
//     .lastName;
// const animaisID = (nome) =>
//   data.employees.find(({ firstName, id }) => firstName === nome || id === nome)
//     .responsibleFor;

function getEmployeesCoverage(test) {}

module.exports = getEmployeesCoverage;
